// Imports the components used to render different types of nodes in the flow editor.

import ScrollAreaNode from "./pkg/nodes/ScrollAreaNode";

// Exports the components used to render different types of nodes in the flow editor.
export const nodeData = {
  scrollArea: ScrollAreaNode,
};
